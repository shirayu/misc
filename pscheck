#!/bin/env zsh

echo "Start checking"

PID=$1
MYHOST=`hostname -s`
while :
do
    if [[ ! -e "/proc/${PID}" ]];then
        echo "Done"
        exit 0
    fi

    echo -n "\r" `date` "    PID ${PID} still lives"
    sleep 1
done


